import React from 'react';
import { observer } from 'mobx-react';

import './App.css';

const App = () => {
  return (
    <React.Fragment>
    </React.Fragment>
  );
};

export default observer(App);
